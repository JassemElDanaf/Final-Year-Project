# Keep Jupyter notebooks as-is (no EOL conversion, avoid noisy diffs)
*.ipynb -text

# Force LF for source code (good for cross-platform)
*.py text eol=lf
*.sh text eol=lf

# Keep CRLF for Windows batch files
*.bat text eol=crlf
